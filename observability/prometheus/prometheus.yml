# DA10 — Prometheus scrape config (monitoring_plan §10)
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: da10-api
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8000"]
