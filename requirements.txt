# DA10 — SearchAPI + ContextAPI build (Owner: Vũ Đức Kiên)
# Cài: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

# ---- API ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# ---- DB (Postgres) ----
psycopg2-binary>=2.9       # ingest (sync)
asyncpg>=0.29              # API (async)
SQLAlchemy>=2.0

# ---- Search / Vector ----
opensearch-py>=2.6,<3.0
qdrant-client>=1.8

# ---- Embedding / Rerank (bge-m3 / bge-reranker-v2-m3) ----
FlagEmbedding>=1.2
sentence-transformers>=2.6
torch>=2.2

# ---- NLP tiếng Việt ----
underthesea>=6.8

# ---- Observability ----
prometheus-client>=0.20
python-json-logger>=2.0

# ---- Evaluation ----
ranx>=0.3.16
httpx>=0.27
pandas>=2.0
numpy>=1.26
openpyxl>=3.1
tiktoken>=0.6

# ---- Utils ----
pyyaml>=6.0
orjson>=3.9
python-dotenv>=1.0
